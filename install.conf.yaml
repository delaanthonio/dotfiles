- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.emacs.d:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.oh-my-zsh: zsh/oh-my-zsh
    ~/.spacemacs.d:
    ~/.zshenv:
    ~/.zshrc:

- shell: []
