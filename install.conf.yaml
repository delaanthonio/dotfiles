- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.doom.d:
    ~/.emacs.d:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.oh-my-zsh: zsh/oh-my-zsh
    ~/.spacemacs.d:
    ~/.zshenv:
    ~/.zshrc:

- shell: []
