- defaults:
    link:
      create: true
      relink: true

- shell: [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.doom.d:
    ~/.emacs.d:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshenv:
    ~/.zshrc:
