- defaults:
    link:
      create: true
      relink: true

- shell: [git submodule update --init --recursive]

- clean: ['~']

- link:
    ~/.config/kitty/kitty.conf: kitty.conf
    ~/.doom.d:
    ~/.emacs.d:
    ~/.ideavimrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshenv:
    ~/.zshrc:
